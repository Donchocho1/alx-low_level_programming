#include "main.h"
/**
 * add - A function that adds two integers
 * @a: first integer to be inputted
 * @b: second integer to be inputed
 *
 * Return: the result of the two int added
 */
int add(int a, int b)
{
	int result;

	result = a + b;
		return (result);

}
