#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @a: THE INPUT VALue
 *
 * Return: an integer, the value entered
 */
int _abs(int a)
{

	if (a < 0)
	{
		return (-a);
	}
	else
		return (a);
}
